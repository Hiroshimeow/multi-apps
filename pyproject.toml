[project]
name = "multi-run-apps"
version = "0.1.0"
description = "Cross-platform launcher for local applications and plugin-style tools"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "psutil>=7.0",
    "pyqt6>=6.10",
    "pyyaml>=6.0",
    "rich>=14.0",
]
